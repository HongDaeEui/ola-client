# OS
.DS_Store

# Env files (each subdir has its own .gitignore for these)
.env
.env.local
.env*.local

# Vercel
.vercel

# node_modules (each subdir has its own)
node_modules/

# harness — do not track runtime state
.agents/state.json
.agents/.stop

# ola-dev orchestrator workspace
_workspace/
_workspace_prev/

# TypeScript incremental build cache
back/dist/tsconfig.tsbuildinfo
